# @workspace/ui

추후 Button, Input, Card 같은 실제 UI 컴포넌트를 배치할 패키지입니다.

초기 단계에서는 `apps/docs`가 로컬 문서와 쇼케이스 역할을 먼저 담당합니다. 컴포넌트 구현이 시작되면 이 패키지에서 컴포넌트를 export하고, `apps/docs`에서 import해 실제 동작과 문서를 함께 확인하는 구조로 확장합니다.
